---
phase: 02-infrastructure-mcp
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .claude/commands/gendoc-lookup.md
  - .claude/commands/gendoc-analyze.md
  - .claude/commands/gendoc-generate.md
  - .claude/commands/gendoc-full.md
autonomous: true

must_haves:
  truths:
    - "L'utilisateur peut taper /gendoc-lookup dans Claude Code et obtenir une interface de recherche de references"
    - "L'utilisateur peut taper /gendoc-analyze dans Claude Code et declencher l'analyse d'un devis PDF"
    - "L'utilisateur peut taper /gendoc-generate dans Claude Code et declencher la generation de slides"
    - "L'utilisateur peut taper /gendoc-full dans Claude Code et declencher le pipeline complet"
    - "Chaque commande /gendoc-* utilise les outils MCP du serveur gendoc"
  artifacts:
    - path: ".claude/commands/gendoc-lookup.md"
      provides: "Skill for product reference lookup"
      contains: "lookup_reference"
    - path: ".claude/commands/gendoc-analyze.md"
      provides: "Skill for devis PDF analysis"
      contains: "analyze_devis"
    - path: ".claude/commands/gendoc-generate.md"
      provides: "Skill for PowerPoint generation"
      contains: "generate_slides"
    - path: ".claude/commands/gendoc-full.md"
      provides: "Skill for full pipeline execution"
      contains: "gendoc"
  key_links:
    - from: ".claude/commands/gendoc-lookup.md"
      to: "src/gendoc/mcp/server.py"
      via: "prompt instructs Claude to use MCP tools"
      pattern: "lookup_reference|list_families|search_references"
    - from: ".claude/commands/gendoc-full.md"
      to: "src/gendoc/mcp/server.py"
      via: "prompt instructs Claude to chain analyze_devis then generate_slides"
      pattern: "analyze_devis.*generate_slides"
---

<objective>
Register `/gendoc-*` slash commands as Claude Code skills that guide Claude to use the MCP server tools for product reference management, devis analysis, and PowerPoint generation.

Purpose: Skills provide the user-facing commands that make the MCP tools discoverable and easy to invoke. Each skill is a markdown file that tells Claude exactly which MCP tools to call and how to present results.

Output: 4 skill files in `.claude/commands/` providing `/gendoc-lookup`, `/gendoc-analyze`, `/gendoc-generate`, and `/gendoc-full` commands.
</objective>

<execution_context>
@C:/Users/RémyBONNIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RémyBONNIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-mcp/02-01-SUMMARY.md
@src/gendoc/mcp/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create gendoc-lookup and gendoc-analyze skills</name>
  <files>.claude/commands/gendoc-lookup.md, .claude/commands/gendoc-analyze.md</files>
  <action>
Create the `.claude/commands/` directory and two skill files.

**`.claude/commands/gendoc-lookup.md`:**
```markdown
# Recherche de references produit Delagrave

Tu es un assistant de recherche de references produit pour le catalogue Delagrave.
Utilise les outils MCP du serveur "gendoc" pour repondre aux demandes de l'utilisateur.

## Instructions

L'utilisateur veut consulter des references produit. Determine ce qu'il cherche et utilise l'outil MCP adapte :

1. **Recherche par code exact** : Utilise l'outil `lookup_reference` avec le code produit.
   - Exemple : l'utilisateur dit "PM-D-H-75" → appelle `lookup_reference(code="PM-D-H-75")`
   - Affiche toutes les informations produit : code, reference, titre, texte, dimensions, images

2. **Liste des familles** : Utilise l'outil `list_families` quand l'utilisateur demande les familles disponibles ou un apercu du catalogue.
   - Affiche les familles avec le nombre de produits dans chaque famille

3. **Recherche partielle** : Utilise l'outil `search_references` quand l'utilisateur donne un terme de recherche partiel.
   - Exemple : "cherche PM-D" → appelle `search_references(query="PM-D")`
   - Affiche la liste des resultats avec code, reference, titre et famille

## Format de reponse

- Presente les resultats de maniere claire et structuree
- Pour un produit unique, affiche toutes les sections (metadata, texte, dimensions, images)
- Pour une liste, affiche un tableau resume
- Si aucun resultat, suggere des recherches alternatives

## Argument

$ARGUMENTS : Le code produit, nom de famille, ou terme de recherche. Si vide, affiche la liste des familles.
```

**`.claude/commands/gendoc-analyze.md`:**
```markdown
# Analyse de devis PDF Delagrave

Tu es un assistant d'analyse de devis PDF pour les produits Delagrave.
Utilise les outils MCP du serveur "gendoc" pour analyser le devis.

## Instructions

L'utilisateur soumet un devis PDF a analyser. Le workflow est :

1. **Recevoir le chemin du fichier PDF** depuis l'argument ou demander a l'utilisateur
2. **Appeler l'outil `analyze_devis`** avec le chemin du PDF
3. **Presenter les resultats** : liste des references extraites, familles detectees, quantites

## Statut actuel

> **Note** : L'analyse de devis sera pleinement implementee en Phase 3.
> Pour l'instant, l'outil est enregistre mais retourne un message de confirmation.
> Cela permet de valider que l'infrastructure MCP fonctionne correctement.

## Argument

$ARGUMENTS : Le chemin vers le fichier PDF du devis a analyser.
```
  </action>
  <verify>
1. Verify files exist: `ls .claude/commands/gendoc-lookup.md .claude/commands/gendoc-analyze.md` — both must exist
2. Verify content contains MCP tool references: `grep -l "lookup_reference" .claude/commands/gendoc-lookup.md` and `grep -l "analyze_devis" .claude/commands/gendoc-analyze.md` — both must match
  </verify>
  <done>
Both skill files exist in `.claude/commands/`, `gendoc-lookup.md` references the 3 reference MCP tools (lookup_reference, list_families, search_references), and `gendoc-analyze.md` references the analyze_devis MCP tool.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create gendoc-generate and gendoc-full skills</name>
  <files>.claude/commands/gendoc-generate.md, .claude/commands/gendoc-full.md</files>
  <action>
**`.claude/commands/gendoc-generate.md`:**
```markdown
# Generation de fiches techniques PowerPoint

Tu es un assistant de generation de fiches techniques PowerPoint pour les produits Delagrave.
Utilise les outils MCP du serveur "gendoc" pour generer les slides.

## Instructions

L'utilisateur veut generer un document PowerPoint de fiches techniques. Le workflow est :

1. **Recevoir la liste des codes produit** depuis l'argument ou demander a l'utilisateur
2. **Verifier chaque reference** avec `lookup_reference` pour confirmer qu'elle existe
3. **Appeler `generate_slides`** avec la liste des codes, le chemin de sortie et le mode

### Parametres

- **product_codes** : Liste des codes produit a inclure (ex: ["PM-D-H-75", "S-A"])
- **output_path** : Chemin du fichier PowerPoint a generer (par defaut: `Delagrave/output/fiches.pptx`)
- **mode** : Mode de generation — "FTI" (Fiches Techniques Individuelles), "CHI" (Cahier), "DOE" (Dossier)

## Statut actuel

> **Note** : La generation PowerPoint sera pleinement implementee en Phase 4.
> Pour l'instant, l'outil est enregistre mais retourne un message de confirmation.
> La verification des references via `lookup_reference` fonctionne deja.

## Argument

$ARGUMENTS : Les codes produit a inclure, separes par des espaces ou virgules.
```

**`.claude/commands/gendoc-full.md`:**
```markdown
# Pipeline complet : Devis PDF vers Fiches Techniques PowerPoint

Tu es un assistant de generation automatique de dossiers de fiches techniques Delagrave.
Utilise les outils MCP du serveur "gendoc" pour executer le pipeline complet.

## Instructions

L'utilisateur soumet un devis PDF et veut obtenir un dossier PowerPoint complet. Le workflow est :

### Etape 1 : Analyse du devis
1. Recevoir le chemin du fichier PDF depuis l'argument ou demander a l'utilisateur
2. Appeler `analyze_devis` avec le chemin du PDF
3. Presenter la liste des references extraites a l'utilisateur

### Etape 2 : Verification des references
1. Pour chaque reference extraite, appeler `lookup_reference` pour verifier qu'elle existe
2. Signaler les references introuvables
3. Presenter un resume : references trouvees, manquantes, familles concernees

### Etape 3 : Confirmation
1. Demander a l'utilisateur de confirmer la liste des fiches a generer
2. Permettre d'ajouter ou retirer des references

### Etape 4 : Generation
1. Appeler `generate_slides` avec les codes confirmes
2. Presenter le resultat : fichier genere, nombre de slides, familles couvertes

## Statut actuel

> **Note** : Le pipeline complet sera fonctionnel apres les Phases 3 (analyse devis), 4 (generation slides) et 5 (assemblage document).
> Pour l'instant, les etapes 1 et 4 retournent des messages de confirmation.
> L'etape 2 (verification des references) fonctionne deja pleinement.

## Argument

$ARGUMENTS : Le chemin vers le fichier PDF du devis. Si vide, demander a l'utilisateur.
```
  </action>
  <verify>
1. Verify files exist: `ls .claude/commands/gendoc-generate.md .claude/commands/gendoc-full.md` — both must exist
2. Verify content: `grep -l "generate_slides" .claude/commands/gendoc-generate.md` and `grep -l "analyze_devis" .claude/commands/gendoc-full.md` — both must match
3. Verify all 4 skill files present: `ls .claude/commands/gendoc-*.md | wc -l` — must be 4
  </verify>
  <done>
All 4 skill files exist in `.claude/commands/`: gendoc-lookup.md, gendoc-analyze.md, gendoc-generate.md, gendoc-full.md. Each references the appropriate MCP tools from the gendoc server. The gendoc-full.md describes the complete pipeline workflow chaining analyze_devis → lookup_reference → generate_slides.
  </done>
</task>

</tasks>

<verification>
1. All skill files exist: `ls .claude/commands/gendoc-*.md` — expect 4 files
2. gendoc-lookup references 3 tools: `grep -c "lookup_reference\|list_families\|search_references" .claude/commands/gendoc-lookup.md` — expect >= 3
3. gendoc-analyze references analyze_devis: `grep -c "analyze_devis" .claude/commands/gendoc-analyze.md` — expect >= 1
4. gendoc-generate references generate_slides: `grep -c "generate_slides" .claude/commands/gendoc-generate.md` — expect >= 1
5. gendoc-full chains all tools: `grep -c "analyze_devis\|lookup_reference\|generate_slides" .claude/commands/gendoc-full.md` — expect >= 3
6. Skills use $ARGUMENTS pattern: `grep -c "ARGUMENTS" .claude/commands/gendoc-*.md` — expect 4
</verification>

<success_criteria>
- 4 skill files exist in `.claude/commands/`
- `/gendoc-lookup` enables reference search through 3 MCP tools
- `/gendoc-analyze` triggers devis PDF analysis via MCP
- `/gendoc-generate` triggers PowerPoint generation via MCP
- `/gendoc-full` chains the complete pipeline (analyze → verify → generate)
- Each skill includes current status notes about which phases will complete the implementation
- Skills use `$ARGUMENTS` for user input
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-mcp/02-02-SUMMARY.md`
</output>
