---
phase: 07-verification-correction-familles
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/gendoc/generators/pptx_generator.py
  - src/gendoc/generators/document_assembler.py
autonomous: false

must_haves:
  truths:
    - "L'utilisateur peut generer une fiche pour chaque famille et verifier visuellement que le layout est correct"
    - "Tous les textes sont lisibles et ne debordent pas des cadres"
    - "Les images sont visibles et correctement positionnees sur les slides"
    - "Les references produit sont affichees correctement pour toutes les familles"
    - "Les placeholders vides sont supprimes (aucun 'Cliquez pour ajouter' visible)"
  artifacts:
    - path: "Delagrave/output/test_phase7_all_families.pptx"
      provides: "Combined document with all 8 families for visual verification"
    - path: "src/gendoc/generators/pptx_generator.py"
      provides: "Final corrected generator with all fixes from visual verification"
  key_links:
    - from: "src/gendoc/generators/pptx_generator.py"
      to: "Delagrave/output/test_phase7_all_families.pptx"
      via: "generate_presentation()"
      pattern: "generate_presentation"
---

<objective>
Visual verification of all 8 families with human checkpoint, followed by targeted fixes for any layout issues discovered during visual inspection.

Purpose: Programmatic verification (Plan 01) confirms structural correctness but cannot catch visual issues like misaligned text, images covering other elements, text too small to read, or wrong proportions. This plan addresses the roadmap requirement that "User can generate a sample fiche for each of the 8 families and visually verify layout correctness."

Output: Verified and corrected generator producing visually correct slides for all families.
</objective>

<execution_context>
@C:/Users/RémyBONNIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RémyBONNIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-verification-correction-familles/07-01-SUMMARY.md
@src/gendoc/generators/pptx_generator.py
@src/gendoc/generators/document_assembler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate comprehensive verification document</name>
  <files>Delagrave/output/test_phase7_all_families.pptx</files>
  <action>
Generate a single comprehensive PowerPoint document containing one product per family (8 products total) plus coating slides. Use the generate_presentation() function with these products:

Product codes (one per family, chosen to have both text and images):
- paillasse: "PCD-A-75" (has long texte + dimensions + image)
- sorbonne: "S-A" (has long texte + 8 dimensions + image)
- revetement: use first product from revetement.md (has 3-zone text split)
- meubles: "ACB120" (has texte + image)
- tables-en: "ELE" (has texte + image)
- equipement: "2CU12G" (has image, ref as dimension)
- elec-sorb: "BARRIEREIMMAT" (has image, ref as dimension)
- complements: "x" (has image, ref as dimension)

Include devis_info for cover page:
```python
devis_info = {
    "numero_devis": "PHASE7-VERIF",
    "date": "2026-02-10",
    "client": "Verification Phase 7",
    "titre_affaire": "Test Complet - Toutes Familles"
}
```

Output to: Delagrave/output/test_phase7_verification.pptx

After generating, print the slide count and any skipped products. Also create a checklist file at Delagrave/output/phase7_checklist.txt listing each slide number and what to verify on it.
  </action>
  <verify>
File Delagrave/output/test_phase7_verification.pptx exists and is non-empty.
```bash
ls -la "H:/IA/Generateur de doc/Delagrave/output/test_phase7_verification.pptx"
```
  </verify>
  <done>Comprehensive verification PPTX generated with all 8 families, with a checklist for manual review</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 8 families</name>
  <files>Delagrave/output/test_phase7_verification.pptx</files>
  <action>
Present the generated test_phase7_verification.pptx to the user for visual inspection. The file contains one product per family (8 families total) in a complete document with cover page and TOC.

User opens Delagrave/output/test_phase7_verification.pptx and checks each product slide for: correct title, visible text, proper reference code, readable dimensions (paillasse/sorbonne), visible images, no "Cliquez pour ajouter" default text, and no text overflow.

Checkpoint elements below provide detailed verification instructions.
  </action>
  <verify>User confirms "approved" or provides specific feedback on issues per slide.</verify>
  <done>User has visually verified all 8 families and either approved or provided actionable feedback</done>
  <what-built>
Generated a comprehensive PowerPoint test document (Delagrave/output/test_phase7_verification.pptx) containing one product per family:

1. Cover page with "Test Complet - Toutes Familles" title
2. Table of contents with all 8 families
3-4. Paillasses: PCD-A-75 (paillasse centrale double)
5-6. Sorbonnes: S-A (sorbonne autoportante)
7-8. Revetements: first revetement product
9-10. Meubles: ACB120 (armoire de collection)
11-12. Tables EN: ELE (table eleve humide)
13-14. Equipement: 2CU12G (cuve gres double)
15-16. Elec Sorbonne: BARRIEREIMMAT (barriere immatérielle)
17-18. Complements: x (revetement compact)
  </what-built>
  <how-to-verify>
Open the file: Delagrave/output/test_phase7_verification.pptx

For EACH product slide, verify these 5 criteria:

**A. Text content:**
- Title is present and readable at top
- Descriptive text is visible (unless product has no text)
- Reference code is displayed correctly
- Page number is shown

**B. Text sizing:**
- No text overflows its placeholder boundary
- Text is auto-sized to fit (may be small but still readable)
- No "Cliquez pour ajouter du texte" default text visible

**C. Images:**
- Product image is visible (if available locally)
- Image is within slide boundaries
- Image does not overlap critical text areas

**D. Layout:**
- Slide looks professionally structured
- Elements are in expected positions (title at top, text in body, image in designated area)

**E. Family-specific checks:**
- Paillasse/Sorbonne: Dimensions table values visible (profondeur, hauteur, etc.)
- Revetement: 3-zone text split working (TEXTE / Mise en oeuvre / Finition)
- Meubles/Tables-en: Texte and image visible
- Equipement/Elec-sorb/Complements: Reference and image visible

Report any issues with specific slide numbers and descriptions.
  </how-to-verify>
  <resume-signal>Type "approved" if all families look correct, or describe issues per slide (e.g., "slide 6: text overlaps image", "slide 14: reference missing")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Apply fixes from visual verification feedback</name>
  <files>src/gendoc/generators/pptx_generator.py, src/gendoc/generators/document_assembler.py</files>
  <action>
If the user approved all families in the checkpoint, this task is a no-op (just regenerate final verification file and confirm).

If the user reported issues, apply targeted fixes. Common fixes anticipated:

**Text overflow:** Adjust text frame properties or font sizes in _populate_slide. May need to add paragraph formatting (reduce font size for specific families).

**Image overlap:** Adjust image position or size in _insert_images. The image coordinates come from the MD files (extracted from VBA), so the fix may need to:
- Clamp image boundaries more aggressively
- Adjust image scale factors for specific layouts

**Missing reference:** Check if the VBA_TO_PLACEHOLDER mapping is correct for that family. Cross-reference with the Metadata PowerPoint section in the MD file.

**Wrong placeholder index:** If a placeholder shows wrong data, the VBA-to-placeholder index mapping may be wrong. Compare the template layout's actual placeholder indices with what VBA_TO_PLACEHOLDER specifies.

After applying fixes:
1. Regenerate test_phase7_verification.pptx with same products
2. Run the programmatic verification from Plan 01 Task 3 again to ensure no regressions
3. Confirm all 8 families still generate without errors

If no fixes needed (user approved), simply regenerate and confirm.
  </action>
  <verify>
Regenerated test file exists:
```bash
ls -la "H:/IA/Generateur de doc/Delagrave/output/test_phase7_verification.pptx"
```
And no family generates with errors (run quick programmatic check on combined file).
  </verify>
  <done>All visual issues from checkpoint are fixed (or none existed), all 8 families generate correct slides, user can produce fiches for any family with confidence</done>
</task>

</tasks>

<verification>
1. test_phase7_verification.pptx contains slides for all 8 families
2. User has visually verified and approved (or issues were fixed and re-verified)
3. No product slide shows "Cliquez pour ajouter" default text
4. Text does not overflow placeholder boundaries on any slide
5. Images are visible and correctly positioned where local files exist
6. Reference codes display correctly for all families
</verification>

<success_criteria>
- User has visually inspected slides for all 8 families and approved
- All text placeholders use auto-sizing (TEXT_TO_FIT_SHAPE)
- No empty placeholder text remains on any product slide
- Images are correctly positioned within slide bounds
- Any visual issues identified during checkpoint have been resolved
</success_criteria>

<output>
After completion, create `.planning/phases/07-verification-correction-familles/07-02-SUMMARY.md`
</output>
