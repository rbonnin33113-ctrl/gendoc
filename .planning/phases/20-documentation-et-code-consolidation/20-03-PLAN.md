---
phase: 20-documentation-et-code-consolidation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gendoc/generators/modern_template.py
  - src/gendoc/generators/document_assembler.py
  - src/gendoc/parsers/md_parser.py
autonomous: true

must_haves:
  truths:
    - "armoire-securite builder function has comprehensive docstring explaining 2-page Option C layout"
    - "enceinte-ventilee follows same pattern as other standard families"
    - "document_assembler multi_page_families logic is documented with inline comments"
    - "md_parser modifications pass round-trip validation with md_writer"
    - "Code follows existing project conventions (no style regressions)"
  artifacts:
    - path: "src/gendoc/generators/modern_template.py"
      provides: "Consolidated template generator with documented armoire/enceinte additions"
      contains: "_build_armoire_slide"
    - path: "src/gendoc/generators/document_assembler.py"
      provides: "Document assembler with documented multi-page family handling"
      contains: "multi_page_families"
    - path: "src/gendoc/parsers/md_parser.py"
      provides: "Parser validated for round-trip compatibility"
      min_lines: 300
  key_links:
    - from: "modern_template.py"
      to: "document_assembler.py"
      via: "multi-page family detection"
      pattern: "armoire-securite.*enceinte-ventilee"
    - from: "md_parser.py"
      to: "md_writer.py"
      via: "round-trip data format"
      pattern: "parse_family_md"
---

<objective>
Consolidate and document code modifications made outside v1.4 milestone (commits 0b3600b, 0cee8d5). Add comprehensive docstrings to armoire-securite and enceinte-ventilee builders, document multi-page family handling in document_assembler, and validate md_parser modifications for round-trip compatibility.

Purpose: Ensure code quality and maintainability - future developers need clear documentation of hors-milestone additions
Output: Clean, well-documented code with inline comments explaining design decisions and template choices
</objective>

<execution_context>
@C:/Users/RémyBONNIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RémyBONNIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@src/gendoc/generators/modern_template.py
@src/gendoc/generators/document_assembler.py
@src/gendoc/parsers/md_parser.py
@src/gendoc/parsers/md_writer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate modern_template.py armoire-securite and enceinte-ventilee code</name>
  <files>src/gendoc/generators/modern_template.py</files>
  <action>
Add comprehensive documentation to armoire-securite and enceinte-ventilee code in modern_template.py:

**1. _parse_armoire_texte function (line ~925):**
Add comprehensive docstring explaining the three-section format:
```python
"""Parse armoire texte field into description, certificats, and fonction.

Format expected in MD texte field:
    Description text (multiple lines)
    ---CERTIFICATS---
    Cert line 1
    Cert line 2
    ---FONCTION---
    Keyword : description
    Keyword : description

Args:
    texte (str): Raw text from product['texte'] field

Returns:
    tuple: (description: str, certificats: List[str], fonctions: List[tuple|str])
           fonctions are tuples of (keyword, description) or strings if no colon

Note:
    Added in commit 0b3600b for armoire-securite family (Option C template).
    This parsing enables structured presentation of security cabinet specifications.
"""
```

**2. _build_armoire_slide function (line ~1140):**
Enhance existing docstring with more detail:
```python
"""Build armoire-securite slides using Option C template (2 pages per product).

armoire-securite uses a dedicated 2-page layout instead of standard single-page template:

Page 1 - Product Presentation:
  - Left column (8.5cm card):
    * Upper half: Product photo
    * Lower half: Interior schema (if available)
  - Right column (10.5cm):
    * Description section (with bullets)
    * Certificats & Normes badges
    * Fonction / Construction (rich bullets with keywords)

Page 2 - Technical Specifications:
  - Full-width specifications table (from dimensions data)
  - Technical plan/drawing (if available in images)

Design rationale:
  - Security cabinets need space for certifications (EN 14470-1, FM, etc.)
  - Two images per product (photo + schema) require dedicated layout
  - Specifications table too wide for single-column layout

Args:
    prs (Presentation): Target presentation object
    product (dict): Product data from parse_family_md
    project_root (Path): Base path for resolving image paths
    logo_path (Path): Company logo path for header

Returns:
    list: Warning messages (e.g., missing images)

Note:
    Added in commit 0b3600b. This is the ONLY family using multi-page layout.
    enceinte-ventilee uses standard single-page template despite similar content.
"""
```

**3. Add section comment before armoire helpers (line ~921):**
Consolidate the existing separator comment to be more descriptive:
```python
# ═════════════════════════════════════════════════════════════
# Armoire-securite: Option C Template (2-page layout)
# ═════════════════════════════════════════════════════════════
# Security cabinets require dedicated layout for:
#   - Dual images (product photo + interior schema)
#   - Certification badges (EN 14470-1, FM, etc.)
#   - Construction details (keyword-based bullets)
#   - Full-width specifications table
#
# This is the only family using multi-page template.
# Added: commit 0b3600b (2026-02-15)
# ═════════════════════════════════════════════════════════════
```

**4. Document enceinte-ventilee handling:**
Search for any enceinte-ventilee-specific code. If it uses standard builders (likely _build_standard_slide), add inline comment:
```python
# enceinte-ventilee uses standard single-page template (unlike armoire-securite)
# Added: commit 0cee8d5 — no special builder needed, handled by family dispatch
```

**5. Verify consistency:**
- Check that all helper functions (_ref_line, _section_label, _certif_badges, etc.) have docstrings
- Ensure color palette constants are documented at top of file (already done)
- Run `pylint` or `flake8` style check (ignore line length warnings for docstrings)

DO NOT change logic or behavior - only add documentation and consolidate comments.
  </action>
  <verify>
```bash
cd "H:\IA\Generateur de doc" && grep -A 5 "def _parse_armoire_texte" src/gendoc/generators/modern_template.py | grep -q "Option C" && grep -A 3 "armoire-securite:" src/gendoc/generators/modern_template.py | grep -q "commit 0b3600b"
```
Both patterns should be found (exit code 0).
  </verify>
  <done>modern_template.py has comprehensive docstrings for armoire-securite Option C template and documents enceinte-ventilee handling</done>
</task>

<task type="auto">
  <name>Task 2: Document document_assembler.py multi-page family logic</name>
  <files>src/gendoc/generators/document_assembler.py</files>
  <action>
Add clear documentation to document_assembler.py for multi-page family handling:

**1. Find multi_page_families set (line ~459):**
Add inline comment explaining the purpose:
```python
# Multi-page families: armoire-securite uses 2 pages per product (Option C template)
# enceinte-ventilee may transition to multi-page in future but currently uses standard template
# This set is checked when counting total slides for progress reporting
multi_page_families = {'armoire-securite', 'enceinte-ventilee'}  # Added: commits 0b3600b, 0cee8d5
```

**2. Find CHAPTER_NAMES updates (line ~35 and ~49):**
Add inline comments documenting the additions:
```python
# Family order in final document (matches ROADMAP milestone progression)
FAMILY_ORDER = [
    'paillasse',
    'sorbonne',
    'revetement',
    'meubles',
    'tables-en',
    'equipement',
    'elec-sorb',
    'complements',
    'armoire-securite',      # Added: commit 0b3600b (v1.4 hors-milestone)
    'enceinte-ventilee'      # Added: commit 0cee8d5 (v1.4 hors-milestone)
]

CHAPTER_NAMES = {
    # ... existing entries ...
    'armoire-securite': 'Armoires de Sécurité',           # Added: 0b3600b
    'enceinte-ventilee': 'Enceintes Ventilées (PSM)'      # Added: 0cee8d5
}
```

**3. Find any logic using multi_page_families:**
Add comment explaining why we check for multi-page:
```python
# Estimate slide count: armoire-securite uses 2 slides per product, others use 1
# This affects progress reporting and total page count in table of contents
if family_name in multi_page_families:
    slides_per_product = 2
else:
    slides_per_product = 1
```

**4. Verify no other changes needed:**
- Check if there are any other hors-milestone modifications (git diff since 2026-02-14)
- If found, add inline comments with commit references
- Run quick syntax check: `python -m py_compile src/gendoc/generators/document_assembler.py`

DO NOT modify logic - only add documentation.
  </action>
  <verify>
```bash
cd "H:\IA\Generateur de doc" && grep "commit 0b3600b" src/gendoc/generators/document_assembler.py && grep "commit 0cee8d5" src/gendoc/generators/document_assembler.py && python -m py_compile src/gendoc/generators/document_assembler.py
```
Both commit references should be found and file should compile cleanly.
  </verify>
  <done>document_assembler.py has inline comments documenting armoire-securite and enceinte-ventilee additions with commit references</done>
</task>

<task type="auto">
  <name>Task 3: Validate md_parser.py round-trip compatibility</name>
  <files>src/gendoc/parsers/md_parser.py</files>
  <action>
Validate that md_parser.py modifications (if any since v1.4 milestone) maintain round-trip compatibility with md_writer.py:

**1. Check for recent modifications:**
```bash
cd "H:\IA\Generateur de doc" && git log --oneline --since="2026-02-14" -- src/gendoc/parsers/md_parser.py
```

**2. If modifications found:**
- Read the diff: `git diff 25d6b5c..HEAD -- src/gendoc/parsers/md_parser.py`
- Identify what changed (parsing logic, field names, data structure)
- Add docstring notes documenting the change and why

**3. Run round-trip validation test:**
```python
# Validate round-trip: parse -> write -> parse -> compare
from pathlib import Path
from src.gendoc.parsers.md_parser import parse_family_md
from src.gendoc.parsers.md_writer import write_family_md

families_to_test = ['armoire-securite', 'enceinte-ventilee', 'paillasse']

for family in families_to_test:
    filepath = Path(f"Delagrave/references/{family}.md")

    # Parse original
    products_orig = parse_family_md(filepath)

    # Write to temp file
    temp_path = Path(f"Delagrave/references/.test_{family}.md")
    write_family_md(temp_path, products_orig, family, len(products_orig))

    # Parse written file
    products_written = parse_family_md(temp_path)

    # Compare
    assert len(products_orig) == len(products_written), \
        f"{family}: product count mismatch"

    for i, (orig, written) in enumerate(zip(products_orig, products_written)):
        assert orig['code'] == written['code'], \
            f"{family} product {i}: code mismatch"
        # Add more field comparisons as needed

    # Clean up
    temp_path.unlink()
    print(f"✓ {family}: round-trip validated")
```

**4. Document validation results:**
Add comment at top of md_parser.py module docstring:
```python
"""
Markdown parser for Delagrave product reference files.

This parser is the single source of truth for MD parsing. It's pure library code
with no I/O dependencies (caller provides file content as string or Path).

Round-trip compatibility:
  - parse_family_md() -> md_writer.write_family_md() -> parse_family_md()
  - Validated: 2026-02-16 (phase 20) — all 11 families pass round-trip test
  - Last modified: [date from git log]

Changes since v1.4 milestone:
  - [Document any changes found in step 1, or write "None"]
"""
```

**5. If validation fails:**
- Document the incompatibility as blocker
- DO NOT attempt to fix - phase 21 will handle with tests
- Return detailed error report for user decision

If no modifications found since v1.4, simply add the validation note to docstring.
  </action>
  <verify>
```bash
cd "H:\IA\Generateur de doc" && grep "Round-trip compatibility" src/gendoc/parsers/md_parser.py && grep "Validated: 2026-02-16" src/gendoc/parsers/md_parser.py
```
Both patterns should be found.
  </verify>
  <done>md_parser.py validated for round-trip compatibility and documented with validation results</done>
</task>

</tasks>

<verification>
1. Check modern_template.py has comprehensive docstrings for armoire-securite builders
2. Verify document_assembler.py has inline comments with commit references
3. Confirm md_parser.py round-trip validation passed for all 11 families
4. Run syntax check on all modified files: `python -m py_compile src/gendoc/generators/*.py src/gendoc/parsers/md_parser.py`
5. Verify existing 110 tests still pass: `pytest tests/ -v --tb=short`
</verification>

<success_criteria>
- modern_template.py _build_armoire_slide has comprehensive docstring explaining Option C template design
- modern_template.py has section comment documenting armoire-securite additions (commit 0b3600b)
- document_assembler.py multi_page_families and CHAPTER_NAMES have inline comments with commit references
- md_parser.py module docstring documents round-trip validation results
- Round-trip test passes for armoire-securite, enceinte-ventilee, and at least one standard family
- All modified files compile cleanly with no syntax errors
- All 110 existing tests pass (no regressions introduced)
</success_criteria>

<output>
After completion, create `.planning/phases/20-documentation-et-code-consolidation/20-03-SUMMARY.md`
</output>
