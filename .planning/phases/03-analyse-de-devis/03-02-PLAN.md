---
phase: 03-analyse-de-devis
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/gendoc/mcp/server.py
  - .claude/commands/gendoc-analyze.md
autonomous: true

must_haves:
  truths:
    - "L'outil MCP analyze_devis retourne un rapport JSON structure quand appele avec un chemin PDF valide"
    - "Le rapport contient header, references avec familles, revetements, forfaits et inconnus"
    - "La commande /gendoc-analyze guide Claude a utiliser l'outil et presenter les resultats de facon lisible"
    - "Un chemin PDF invalide retourne un message d'erreur clair, pas un crash"
  artifacts:
    - path: "src/gendoc/mcp/server.py"
      provides: "Outil analyze_devis connecte a devis_analyzer.analyze_devis() avec gestion d'erreurs"
      contains: "from gendoc.parsers.devis_analyzer import analyze_devis"
    - path: ".claude/commands/gendoc-analyze.md"
      provides: "Skill mise a jour avec instructions pour presenter le rapport d'analyse"
      contains: "references"
  key_links:
    - from: "src/gendoc/mcp/server.py"
      to: "src/gendoc/parsers/devis_analyzer.py"
      via: "import et appel de analyze_devis()"
      pattern: "from gendoc\\.parsers\\.devis_analyzer import"
    - from: ".claude/commands/gendoc-analyze.md"
      to: "src/gendoc/mcp/server.py"
      via: "Skill instruits Claude a appeler l'outil MCP analyze_devis"
      pattern: "analyze_devis"
---

<objective>
Connecter le module devis_analyzer au serveur MCP et mettre a jour la skill /gendoc-analyze pour que l'utilisateur puisse analyser un devis PDF via Claude Code et obtenir un rapport structure.

Purpose: Transformer le stub MCP en outil fonctionnel et rendre l'analyse accessible a l'utilisateur final via la commande /gendoc-analyze.
Output: MCP server mis a jour avec analyze_devis fonctionnel, skill mise a jour avec instructions de presentation du rapport.
</objective>

<execution_context>
@C:/Users/RémyBONNIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RémyBONNIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@H:/IA/Generateur de doc/.planning/PROJECT.md
@H:/IA/Generateur de doc/.planning/ROADMAP.md
@H:/IA/Generateur de doc/.planning/STATE.md
@H:/IA/Generateur de doc/.planning/phases/03-analyse-de-devis/03-CONTEXT.md
@H:/IA/Generateur de doc/.planning/phases/03-analyse-de-devis/03-01-SUMMARY.md
@H:/IA/Generateur de doc/src/gendoc/mcp/server.py
@H:/IA/Generateur de doc/.claude/commands/gendoc-analyze.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Connecter analyze_devis au serveur MCP avec gestion d'erreurs</name>
  <files>src/gendoc/mcp/server.py</files>
  <action>
Modifier `src/gendoc/mcp/server.py` pour remplacer le stub `analyze_devis` par l'implementation reelle.

**Modifications:**

1. Ajouter l'import en haut du fichier:
```python
from gendoc.parsers.devis_analyzer import analyze_devis as run_analyze_devis
```
(Nommer l'import `run_analyze_devis` pour eviter conflit avec la fonction MCP `analyze_devis`.)

2. Remplacer le corps de la fonction `analyze_devis(pdf_path: str) -> str`:
```python
@mcp.tool()
async def analyze_devis(pdf_path: str) -> str:
    """
    Analyze a devis PDF and extract product references with families and coatings.

    Args:
        pdf_path: Path to the devis PDF file

    Returns:
        JSON string with analysis results: header, references, revetements, forfaits, inconnus.
    """
    path = Path(pdf_path)

    # Resolve relative paths from project root
    if not path.is_absolute():
        path = Path(__file__).resolve().parent.parent.parent.parent / path

    if not path.exists():
        return json.dumps({"error": f"Fichier PDF non trouve: {pdf_path}"}, ensure_ascii=False)

    try:
        result = run_analyze_devis(path, REFERENCES_DIR)
        return json.dumps(result, ensure_ascii=False, indent=2)
    except ValueError as e:
        return json.dumps({"error": f"Erreur de lecture du PDF: {str(e)}"}, ensure_ascii=False)
    except Exception as e:
        return json.dumps({"error": f"Erreur inattendue: {str(e)}"}, ensure_ascii=False)
```

3. Mettre a jour la docstring du module pour retirer les mentions "stub - Phase 3":
```python
"""
MCP server for gendoc - exposes all gendoc tools via Model Context Protocol.

This server provides Claude Code with direct access to:
- Reference lookup and search
- Devis PDF analysis (extract references, families, coatings)
- PowerPoint generation (stub - Phase 4)
- Reference management (stub - future)
"""
```

**Ne PAS toucher aux autres outils** — seul analyze_devis change.
  </action>
  <verify>
Test via Python direct (pas besoin de demarrer le serveur MCP complet):
```bash
cd "H:/IA/Generateur de doc" && python -c "
import asyncio, json
from gendoc.mcp.server import analyze_devis

# Test avec chemin relatif
result = asyncio.run(analyze_devis('Delagrave/Devis - Modeles/Devis Test.pdf'))
data = json.loads(result)
print(f'Keys: {list(data.keys())}')
assert 'error' not in data, f'Error: {data[\"error\"]}'
assert 'references' in data
assert len(data['references']) > 0
print(f'References: {len(data[\"references\"])}')
print(f'Revetements: {len(data[\"revetements\"])}')
print(f'Forfaits: {len(data[\"forfaits\"])}')
print(f'Inconnus: {len(data[\"inconnus\"])}')

# Test avec chemin invalide
result_err = asyncio.run(analyze_devis('inexistant.pdf'))
data_err = json.loads(result_err)
assert 'error' in data_err, 'Missing error for invalid path'
print(f'Error message: {data_err[\"error\"]}')

print('ALL CHECKS PASSED')
"
```
  </verify>
  <done>L'outil MCP analyze_devis retourne un rapport JSON structure pour un PDF valide et un message d'erreur JSON pour un chemin invalide.</done>
</task>

<task type="auto">
  <name>Task 2: Mettre a jour la skill /gendoc-analyze avec instructions de presentation du rapport</name>
  <files>.claude/commands/gendoc-analyze.md</files>
  <action>
Remplacer le contenu de `.claude/commands/gendoc-analyze.md` par des instructions completes pour Claude Code.

**Contenu de la skill mise a jour:**

```markdown
# Analyse de devis PDF Delagrave

Tu es un assistant d'analyse de devis PDF pour les produits Delagrave.
Utilise l'outil MCP `analyze_devis` du serveur "gendoc" pour analyser le devis.

## Instructions

L'utilisateur soumet un devis PDF a analyser. Voici le workflow :

1. **Recevoir le chemin du fichier PDF** depuis $ARGUMENTS ou demander a l'utilisateur
2. **Appeler l'outil `analyze_devis`** avec le chemin du PDF
3. **Presenter les resultats** selon le format ci-dessous

## Format de presentation du rapport

### En-tete du devis
Afficher le numero de devis, la date et le client en debut de rapport.

### References produit trouvees
Presenter sous forme de tableau Markdown :
| Code article | Famille | Revetement |
Les references avec revetement detecte doivent avoir le code revetement dans la colonne Revetement.

### Fiches revetement a generer
Lister les fiches revetement qui devront etre incluses dans le dossier PowerPoint.
Format: code revetement + titre complet (ex: "GE - Glace emaillee").

### Forfaits ignores
Lister les codes identifies comme forfaits (transport, pose, etc.) qui ne sont pas des produits.

### References inconnues
Si des codes n'ont pas ete trouves dans les fichiers de references, les lister clairement.
Signaler a l'utilisateur qu'il peut ajouter ces references manuellement via /gendoc-lookup.

## Argument

$ARGUMENTS : Le chemin vers le fichier PDF du devis a analyser.
```

Supprimer toute mention de "Phase 3", "stub" ou "sera implemente plus tard" — l'outil est maintenant fonctionnel.
  </action>
  <verify>
Verifier que le fichier contient les bonnes instructions:
```bash
cd "H:/IA/Generateur de doc" && python -c "
content = open('.claude/commands/gendoc-analyze.md', encoding='utf-8').read()
assert 'analyze_devis' in content, 'Missing analyze_devis reference'
assert 'Famille' in content, 'Missing Famille in format'
assert 'Revetement' in content or 'revetement' in content, 'Missing revetement section'
assert 'Forfaits' in content, 'Missing forfaits section'
assert 'inconnues' in content.lower() or 'inconnus' in content.lower(), 'Missing unknowns section'
assert 'stub' not in content.lower(), 'Still contains stub reference'
assert 'Phase 3' not in content, 'Still contains Phase 3 reference'
assert '\$ARGUMENTS' in content, 'Missing \$ARGUMENTS'
print('ALL CHECKS PASSED')
"
```
  </verify>
  <done>La skill /gendoc-analyze guide Claude a presenter un rapport structure avec tableau de references, fiches revetement, forfaits et references inconnues. Aucune mention de stub.</done>
</task>

</tasks>

<verification>
1. `python -c "from gendoc.mcp.server import analyze_devis"` — import OK
2. L'outil MCP retourne du JSON valide pour le PDF test
3. L'outil MCP retourne une erreur JSON pour un chemin invalide
4. La skill ne contient plus de mention "stub" ou "Phase 3"
5. La skill guide la presentation avec tableau de references, revetements, forfaits et inconnus
6. Le serveur MCP demarre toujours sans erreur: `python -c "from gendoc.mcp.server import mcp; print('OK')"`
</verification>

<success_criteria>
- L'outil MCP analyze_devis est fonctionnel (plus un stub)
- Un PDF valide retourne un rapport JSON avec references, revetements, forfaits, inconnus
- Un chemin invalide retourne un message d'erreur JSON propre
- La skill /gendoc-analyze guide la presentation du rapport de facon lisible
- Le serveur MCP demarre sans erreur avec tous les outils
</success_criteria>

<output>
After completion, create `.planning/phases/03-analyse-de-devis/03-02-SUMMARY.md`
</output>
