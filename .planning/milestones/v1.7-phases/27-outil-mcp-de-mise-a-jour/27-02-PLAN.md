---
phase: 27-outil-mcp-de-mise-a-jour
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/gendoc/mcp/server.py
autonomous: true

must_haves:
  truths:
    - "Un outil MCP update_gendoc est enregistre et accessible depuis Claude"
    - "L'outil appelle run_update() avec github_repo et github_token depuis la config chargee"
    - "L'outil retourne un JSON avec status, old_version, new_version, steps_completed, resume"
    - "En cas d'erreur dans run_update(), l'outil retourne le dict d'erreur sans crasher"
    - "Le champ resume est toujours present pour que Claude puisse resumer au user"
  artifacts:
    - path: "src/gendoc/mcp/server.py"
      provides: "Outil MCP update_gendoc enregistre via @mcp.tool()"
      contains: "async def update_gendoc"
  key_links:
    - from: "src/gendoc/mcp/server.py"
      to: "src/gendoc/utils/auto_updater.py"
      via: "import run_update"
      pattern: "from gendoc\\.utils\\.auto_updater import run_update"
    - from: "src/gendoc/mcp/server.py"
      to: "gendoc.json config"
      via: "_config.get pour github_repo et github_token"
      pattern: "_config\\.get.*github"
---

<objective>
Ajouter l'outil MCP `update_gendoc` dans server.py qui appelle le module auto_updater et retourne le resultat a Claude.

Purpose: C'est le point d'entree utilisateur -- "un clic" dans Claude pour lancer la mise a jour.
Output: server.py modifie avec le nouvel outil MCP fonctionnel.
</objective>

<execution_context>
@C:/Users/RémyBONNIN/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/RémyBONNIN/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-outil-mcp-de-mise-a-jour/27-01-SUMMARY.md
@src/gendoc/mcp/server.py
@src/gendoc/utils/auto_updater.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ajouter l'outil MCP update_gendoc dans server.py</name>
  <files>src/gendoc/mcp/server.py</files>
  <action>
Modifier `src/gendoc/mcp/server.py` pour ajouter l'outil MCP `update_gendoc`.

**1. Ajouter l'import en haut du fichier (apres les imports existants de gendoc.utils):**
```python
from gendoc.utils.auto_updater import run_update
```

**2. Ajouter la fonction MCP apres le dernier outil existant (delete_reference) et avant main():**

```python
@mcp.tool()
async def update_gendoc() -> str:
    """
    Lance la mise a jour automatique de gendoc depuis GitHub.

    Detecte si Git est installe (l'installe si besoin via winget),
    puis execute git pull (ou clone initial) et pip install -e .
    Le resultat inclut les versions avant/apres et les etapes realisees.

    IMPORTANT: Apres une mise a jour reussie, il faut redemarrer Claude
    pour que les changements soient pris en compte.

    Returns:
        JSON string avec status, old_version, new_version, steps_completed, resume.

    Example:
        update_gendoc() -> {"status": "success", "old_version": "2.0.0", "new_version": "2.1.0", ...}
    """
    try:
        result = run_update(
            github_repo=_config.get("github_repo", ""),
            github_token=_config.get("github_token", "") or None,
        )
        return json.dumps(result, ensure_ascii=False, indent=2)
    except Exception as e:
        return json.dumps({
            "status": "error",
            "error": f"Erreur inattendue lors de la mise a jour: {str(e)}",
            "resume": f"ECHEC mise a jour: {str(e)}"
        }, ensure_ascii=False)
```

**Points importants:**
- L'outil ne prend AUCUN parametre -- tout vient de la config chargee au demarrage (_config)
- Le try/except externe est une securite supplementaire -- run_update() gere deja ses propres erreurs
- Pas de `_require_admin()` -- tous les utilisateurs doivent pouvoir mettre a jour
- Le champ `resume` est TOUJOURS present dans la reponse (soit depuis run_update, soit depuis le except)
- L'outil n'a PAS besoin de _current_logger ni de PipelineLogger (pas une generation de fiches)

**3. Verifier que l'import fonctionne et que le serveur MCP peut se lancer:**
```bash
python -c "from gendoc.mcp.server import mcp; tools = mcp._tool_manager._tools; print([t for t in tools.keys()])"
```
La liste doit contenir `update_gendoc`.

**4. Lancer la suite de tests complete pour verifier pas de regression:**
```bash
python -m pytest tests/ -v --tb=short
```
Tous les tests doivent toujours passer (>= 175).
  </action>
  <verify>
    python -c "from gendoc.mcp.server import mcp; tools = mcp._tool_manager._tools; assert 'update_gendoc' in tools; print('update_gendoc registered OK')"
    python -m pytest tests/ -v --tb=short
  </verify>
  <done>
    L'outil MCP update_gendoc est enregistre dans le serveur MCP. L'import de auto_updater fonctionne. La suite de tests complete passe sans regression (>= 175 tests). L'outil retourne toujours un JSON avec un champ "resume".
  </done>
</task>

</tasks>

<verification>
1. `python -c "from gendoc.mcp.server import mcp; ..."` -- update_gendoc dans la liste des outils
2. `python -m pytest tests/ -v --tb=short` -- suite complete sans regression
3. Verifier visuellement que la docstring de update_gendoc mentionne le redemarrage necessaire
</verification>

<success_criteria>
- update_gendoc apparait dans les outils MCP enregistres
- L'outil appelle run_update() avec les bons parametres depuis _config
- Le try/except garantit qu'aucune exception ne remonte au serveur MCP
- Suite complete >= 175 tests, 0 echec
</success_criteria>

<output>
After completion, create `.planning/phases/27-outil-mcp-de-mise-a-jour/27-02-SUMMARY.md`
</output>
